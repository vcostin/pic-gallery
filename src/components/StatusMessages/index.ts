/**
 * StatusMessages package
 * 
 * This module re-exports UI components from the main StatusMessages.tsx file.
 */

// Export components from parent directory
export { 
  LoadingSpinner, 
  ErrorMessage, 
  SuccessMessage, 
  EmptyState, 
  SkeletonLoader 
} from '@/components/StatusMessages';
